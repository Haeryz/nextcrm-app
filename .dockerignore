# Dependencies (reinstalled in Docker)
node_modules
.pnpm-store

# Next.js build output (built inside Docker)
.next
out

# Development & test files
.env*
!.env.example
!.env.production.example
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
__tests__
tests
playwright-report
test-results
coverage
tmp
output
backups
invoice_payment
tsconfig.tsbuildinfo

# Local runtime state and credentials (never send to the build context)
.wwebjs_auth
.wwebjs_cache
*.session
*.sqlite
*.sqlite3

# Local business exports and office files
*.xlsx
*.xls
*.csv

# Git & CI
.git
.gitignore
.github

# Editor & OS
.vscode
.idea
*.swp
.DS_Store
Thumbs.db

# Docs (not needed in image)
*.md
docs

# Docker files themselves (avoid COPY . . including these)
# (kept intentionally so build context is correct)
